@model AtelierAuto.Models.ComandaMvc

@{
    ViewBag.Title = "CautaComanda";
}
<h2>CautareComanda</h2>
@using (Html.BeginForm("CautaComanda", "Home", FormMethod.Post, new { @class = "linkStyle", mvc = @Model }))
{
    <div>
        <fieldset>
            <div class="editor-label">
                @Html.LabelFor(c => c.masina.Model)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(c => c.masina.Model)
            </div>
          
            <input type="submit" value="CautareComanda" />
        </fieldset>
    </div>
}